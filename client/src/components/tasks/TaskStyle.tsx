import { mergeStyles, mergeStyleSets } from 'office-ui-fabric-react/lib/Styling';

export const taskStyles = mergeStyleSets({
    root: {},
    taskTextField: {
        display: 'block',
        marginBottom: '10px'
    }
})